# 行尾。默认交给 git 自动处理，但这两个必须钉死：
* text=auto

# install.sh 必须是 LF —— CRLF 会让 mac/linux 报 "bad interpreter: /usr/bin/env^M"
*.sh text eol=lf

# install.ps1 必须是 CRLF + UTF-8 BOM —— PowerShell 5.1 没有 BOM 会把中文当 ANSI 读，
# 直接解析失败。不要把 BOM 去掉。
*.ps1 text eol=crlf

# Python / 数据文件用 LF，跨平台一致
*.py text eol=lf
*.json text eol=lf
*.md text eol=lf
*.html text eol=lf

# 二进制
*.png binary
*.jpg binary
*.jpeg binary
*.mp4 binary
*.mov binary
